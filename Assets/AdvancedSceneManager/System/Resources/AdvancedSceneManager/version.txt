2.1.39
2.1.39
- Standalone startup scenes were being opened twice during startup, once before startup collections and once after, changed to only persistent scenes before, and non-persistent after.
- Re-added whitelist, can be found in same places as blacklist, in assets settings panel, and by right clickling on a scene in import popup.
- Re-added global update check on timer.
- Fixed scene helper button alignment. 
- Added toggle for profile button and scene helper button visibility in appearance settings.
- Added duplicate button to profile picker.
- Added toggle to select all in import popup.
- Fixed null ref when retrieving saved searches on startup.
- Fixed fallback scene generation error when asset database is being rebuilt, requiring a recompile / restart to initialize ASM after re-clone of git repo.

2.1.38
- Updated description for startup scene, in settings, that was outdated.

2.1.37
- Additive collections did not set active scene, added option to do so.

2.1.36
- Fixed one of the two SceneUtility.Move() overloads calling itself instead of the other one.

2.1.35
- Removed examples link since it wasn't very obvious and it was also redundant since it just linked to docs anyways. 
- Header for doc links changed to "Documentation and examples".

2.1.34
- Fixed TextField.selectAllOnMouseUp and .selectAllOnFocus on search bar, that are not supported below unity 2022.1.

2.1.33
- Possible fix for ASM opening a collection set to Auto during startup, when another collection already had Open set.

2.1.32
- Added search function for scenes and collections (configurable in settings > editor).
- Fixed update check writing old patch notes to log when checking.

2.1.31
- Fixed duplicate standalone scene when set to open on play mode enter.
- Fixed dragging scene to collection header in window.
- Fixed scene dropzone in collections not hiding when it should.

2.1.30 and earlier
- Testing a improved initialization process for ASM.
- Fixed potential issue with legacy mode clean up.
- Added 'ASM/Import scene...', and 'ASM/Unimport scene...' to asset context menu.
- Added some text to make rules for blacklist a bit clearer.
- Fixed issue where blacklist would not update until domain reload.
- Fixed issue where import popup would display for dynamic collection scenes.
- Improved scene import / unimport speed.
- Fixed issue where empty Untitled scenes would not be closed when opening a collection.
- Made ASMInfo.IsLegacySetup() faster.
- Added toggle to disable legacy mode check during editor initialize.
- Fixed compilation errors when new input system was enabled, but not installed.
- Moved Scene merge context menu items into 'Advanced Scene Manager' submenu.
- Fixed issue where SceneField would not handle drag drop.
- Added collection pinning, drag drop supported.
- Fixed issue where legacy mode prompt would appear multiple times.
- Fixed issue where Untitled scene would be restored after exiting play mode, when domain reload was turned on.
- Fixed issue removing legacy assets, when upgrading to 2.0 in legacy mode prompt.
- Added fade animation to in-game toolbar, fixed flickering on default pause screen fade animation.
- Fixed issue where AssetDatabaseUtility.CreateFolder did not work properly on linux (which also caused asm settings to not be included in build).
- Fixed delay before scene manager window is displayed, that was introduced in earlier patch.
- Fixed issue where scene setup would not be saved when exiting play mode.
- Fixed issue where buttons in scene import popups would be disabled on popup open.
- Changed standalone scene startup to open before other scenes0
- Fixed standalone startup scenes not opening using toolbar play button.
- Added SceneManager.runtime.scenePreloadFinished.
- Fixed cross-scene references not restoring when finishing preload.
- Fixed null ref on collection re-order.
- Fixed Scene helper not being able to re-generate when AdvancedSceneManager folder does not exist.
- Improved performance of CollectionItem.SetupSceneDropZone().
- Fixed appearance settings not refreshing elements dynamically.
- Disabled attempted generation of assets in profiler process.
- Added option to hide overview button.
- Fixed hidden buttons in scene manager window header, in unity 6.